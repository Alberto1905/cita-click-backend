# =============================================================================
# CONFIGURACIÓN DE STRIPE
# =============================================================================
# IMPORTANTE: Este es un archivo de EJEMPLO
# Copia este archivo a application-local.properties y configura tus valores reales
# NUNCA subas las API keys reales a Git

# Stripe Secret Key (Test o Production)
stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_XXXXX}

# Webhook Secret (obtenido del Dashboard de Stripe → Developers → Webhooks)
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_XXXXX}

# Porcentaje de comisión de la plataforma (ejemplo: 5.0 = 5%)
stripe.platform.fee.percentage=5.0

# Nombre de la plataforma (aparece en Stripe)
stripe.platform.name=Cita Click

# =============================================================================
# CONFIGURACIÓN DE TWILIO
# =============================================================================

# Account SID (Dashboard de Twilio)
twilio.account.sid=${TWILIO_ACCOUNT_SID:ACXXXXX}

# Auth Token (Dashboard de Twilio)
twilio.auth.token=${TWILIO_AUTH_TOKEN:your_auth_token_here}

# Número de WhatsApp de Twilio (formato: whatsapp:+14155238886)
# Para sandbox: whatsapp:+14155238886
# Para producción: whatsapp:+tu_numero_aprobado
twilio.whatsapp.from=${TWILIO_WHATSAPP_FROM:whatsapp:+14155238886}

# =============================================================================
# CONFIGURACIÓN DE SENDGRID
# =============================================================================

# SendGrid API Key
sendgrid.api.key=${SENDGRID_API_KEY:SG.XXXXX}

# Email remitente (debe estar verificado en SendGrid)
sendgrid.from.email=${SENDGRID_FROM_EMAIL:noreply@citaclick.com}

# Nombre del remitente
sendgrid.from.name=Cita Click

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
#
# 1. VARIABLES DE ENTORNO:
#    Todas las configuraciones sensibles usan ${VAR:default}
#    Puedes configurarlas como variables de entorno o en application-local.properties
#
# 2. AMBIENTES:
#    - application-local.properties: desarrollo local (no subir a Git)
#    - application-dev.properties: desarrollo (servidor)
#    - application-prod.properties: producción
#
# 3. STRIPE:
#    - Test keys empiezan con sk_test_
#    - Production keys empiezan con sk_live_
#    - NUNCA usar production keys en desarrollo
#
# 4. WEBHOOKS:
#    - Configurar en Stripe Dashboard
#    - URL: https://tuapp.com/api/v1/webhooks/stripe
#    - Eventos necesarios:
#      * payment_intent.succeeded
#      * payment_intent.payment_failed
#      * charge.refunded
#      * account.updated
#      * payout.paid
#      * invoice.paid
#      * invoice.payment_failed
#      * customer.subscription.deleted
#
# 5. TESTING LOCAL DE WEBHOOKS:
#    Usar Stripe CLI:
#    stripe listen --forward-to localhost:8080/api/v1/webhooks/stripe
#
# =============================================================================
